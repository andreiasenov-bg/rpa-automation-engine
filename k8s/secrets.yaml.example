apiVersion: v1
kind: Secret
metadata:
  name: rpa-engine-secrets
  namespace: rpa-engine
  labels:
    app.kubernetes.io/name: rpa-engine
type: Opaque
stringData:
  # IMPORTANT: Do NOT commit actual secrets. Generate strong random values for production.
  # Generate with: openssl rand -base64 32
  JWT_SECRET_KEY: "CHANGE_ME_IN_PRODUCTION_USE_SEALED_SECRETS"
  ENCRYPTION_KEY: "CHANGE_ME_IN_PRODUCTION_USE_SEALED_SECRETS"
  POSTGRES_PASSWORD: "changeme"
  # Claude API key (optional)
  CLAUDE_API_KEY: ""
  # Deployer authentication token
  DEPLOY_TOKEN: "CHANGE_ME_IN_PRODUCTION"
