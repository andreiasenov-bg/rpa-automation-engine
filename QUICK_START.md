# 🚀 Бързо стартиране — RPA Automation Engine

## Какво ти трябва

1. **Docker Desktop** — [Изтегли тук](https://www.docker.com/products/docker-desktop/)
2. **Git** — за клониране на репото

## Стартиране (3 стъпки)

### 1. Клонирай проекта

```bash
git clone https://github.com/andreiasenov-bg/rpa-automation-engine.git
cd rpa-automation-engine
```

### 2. Стартирай setup скрипта

```bash
chmod +x setup.sh
./setup.sh
```

Скриптът автоматично:
- Проверява дали Docker е инсталиран и стартиран
- Създава `.env` файл с default настройки
- Билдва Docker images (2-5 мин първия път)
- Стартира всички 6 контейнера (PostgreSQL, Redis, Backend, Celery Worker, Celery Beat, Frontend)
- Изпълнява database миграции и seed (създава admin акаунт)
- Изчаква всички услуги да са ready

### 3. Отвори в браузъра

```
http://localhost:3000
```

**Вход:**
- Email: `admin@rpa-engine.local`
- Парола: `admin123!`

---

## Какво получаваш

| Услуга | URL | Описание |
|--------|-----|----------|
| **Frontend** | http://localhost:3000 | React UI — Dashboard, Workflows, Executions |
| **Backend API** | http://localhost:8000 | FastAPI REST API |
| **API Docs** | http://localhost:8000/docs | Swagger UI с всички 127 endpoints |
| **PostgreSQL** | localhost:5432 | Database (user: `rpa_user`, pass: `rpa_secret_dev`) |
| **Redis** | localhost:6379 | Cache + Message broker |

## Полезни команди

```bash
# Виж логовете
make dev-logs

# Спри всичко
make down

# Стартирай отново
make dev

# Пребилдвай (след промени в кода)
make dev-build

# Database shell
make db-shell

# Изтрий всичко и започни отначало
make clean
```

## Проблеми?

### "Port 5432 is already in use"
Имаш локален PostgreSQL. Или го спри, или промени порта в `docker-compose.yml`.

### "Port 3000 is already in use"
Друго приложение ползва порт 3000. Промени `"3000:80"` в `docker-compose.yml` на `"3001:80"`.

### Backend не стартира
Провери логовете: `docker compose logs backend`

### Бавен първи старт
Нормално е — Docker сваля images и билдва за пръв път. При следващо стартиране е < 30 секунди.

---

## Структура на контейнерите

```
┌──────────────────────────────────────────────┐
│                  Frontend                     │
│              nginx (port 3000)                │
│   ┌──────────────────────────────────────┐   │
│   │  React SPA + API proxy → backend     │   │
│   └──────────────────────────────────────┘   │
└───────────────────────┬──────────────────────┘
                        │ /api/, /ws/
┌───────────────────────▼──────────────────────┐
│                  Backend                      │
│          FastAPI + Uvicorn (port 8000)        │
│   ┌──────────┐  ┌───────────┐  ┌─────────┐  │
│   │ REST API │  │ WebSocket │  │ Alembic │  │
│   └────┬─────┘  └─────┬─────┘  └────┬────┘  │
│        │              │              │        │
└────────┼──────────────┼──────────────┼────────┘
         │              │              │
┌────────▼──────┐ ┌─────▼──────┐ ┌────▼────────┐
│  PostgreSQL   │ │   Redis    │ │   Celery    │
│  (port 5432)  │ │ (port 6379)│ │  Worker +   │
│  Data store   │ │ Cache/MQ   │ │   Beat      │
└───────────────┘ └────────────┘ └─────────────┘
```
