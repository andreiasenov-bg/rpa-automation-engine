# ── RPA Automation Engine — Environment Variables ──
# Copy to .env:  cp .env.example .env
# For local development, the defaults below work out of the box!

# ── Database ──
POSTGRES_DB=rpa_engine
POSTGRES_USER=rpa_user
POSTGRES_PASSWORD=rpa_secret_dev

# ── Security ──
# Change in production! Generate: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=dev-secret-key-change-in-production
# Fernet key — generate your own: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=8C2qrXuG9jPsD6Pr_ZCa6Ud-LApiLta1j55MhbyOGS8=

# ── Application ──
ENVIRONMENT=development
LOG_LEVEL=info
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# ── Admin User (created automatically on first start) ──
ADMIN_EMAIL=admin@rpa-engine.com
ADMIN_PASSWORD=admin123!

# ── Optional: Claude AI ──
# ANTHROPIC_API_KEY=sk-ant-...

# ── Optional: Email notifications ──
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_FROM=noreply@rpa.example.com

# ── Optional: Slack integration ──
# SLACK_WEBHOOK_URL=
# SLACK_BOT_TOKEN=
